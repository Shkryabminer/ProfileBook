<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ProfileBook.Views.MainListView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com">
    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding AddNewProfileCommand}" Text="Add" />
    </ContentPage.ToolbarItems>
    <StackLayout Orientation="Vertical">
        <ListView
            x:Name="profilesListView"
            ItemsSource="{Binding Path=Profiles}"
            SelectedItem="{Binding SelectedProfile}">
            <ListView.Behaviors />
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout Orientation="Horizontal">
                                <Image HeightRequest="40" WidthRequest="100" />
                                <StackLayout Orientation="Vertical">
                                    <Label Text="{Binding Path=FirstName}" TextColor="Black" />
                                    <Label Text="{Binding Path=SecondName}" TextColor="Black" />

                                </StackLayout>

                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>